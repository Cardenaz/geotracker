import { UseCase } from "../../../../shared/core/UseCase";

export class StartTrackingWasherUseCase implements UseCase<any, any> {
    execute(request?: any): Promise<any> {
        throw new Error("Method not implemented.");
    }
}